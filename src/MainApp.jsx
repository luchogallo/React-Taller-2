import { Contador } from "./Contador";

export const MainApp = () => {
    return(
        <div className="App">
            <header className="App-header">
                <Contador />
            </header>
        </div>
    );
}